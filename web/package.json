{
  "name": "web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "vitest run",
    "db:build": "cd ../pipeline && go run ./cmd/migrate -db rankings.db -dir ../db/migrations && go run ./cmd/seed -db rankings.db -dir ../db/seed && go run ./cmd/scrape -db rankings.db -fixture internal/scraper/testdata/ranking_container_14300895_10weights.json && go run ./cmd/resolve -db rankings.db"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "nuxt": "^3.21.8",
    "vue": "^3.5.39"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "vitest": "^4.1.9"
  }
}
